/*
  Check TypeScript Version:

  - go to cmd
  - type "tsc -v" or "tsc --version"
  - go to website if not available
  - 1. Global: "npm install -g typescript"
  - 2. Local: "npm install typescript --save-dev"

  - we prefer to use "1" i.e. global
  - paste "npm install -g typescript"
    
  - paste "npm i -g ts-node"
  - it will install "ts-node"
  - why we install it: to run typescript files

  - install extention:
  - "JavaScript and TypeScript Nightly"
  - why: to run typescript files
  - "Code Runner"
  - why: to run typescript files

  Note: Make sure you have Node JS installed
*/

/*
  If code does not work after the installation of "code runner" and "ts-node",
  then type,
  "tsc --init"

  this will create the "tsconfig.json" file
  this file is used to configure the typescript compiler
*/

/*
  Modern Coding Platform like VS Code,
  comes with a built-in typescript compiler

  the node compiler or the system compiler
  does not support the typescript compiler i.e. ".ts" extension
*/

console.log("message");

